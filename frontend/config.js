/* Production: set to your API base including /api (e.g. https://your-service.onrender.com/api). Local dev can omit this — the app uses http://localhost:4000/api on localhost. */
window.KEJA_CONFIG = {
  API_BASE_URL: "https://mtaani-dq58.onrender.com/api"
};